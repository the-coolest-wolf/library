import { SafeAreaView, View, StyleSheet, Text, Image, ScrollView } from 'react-native'
import LinkButton from '../components/LinkButton'

export default function HomeTemplate({ details }){
    const { name, reading, favorite, worldwide, recommendation, rising, author, topTen} = details.params;

    <Text>
        
    </Text>
}